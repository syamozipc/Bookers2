<div class="container">
	<div class="row">
		<div class="col-xs-3">
			<%= render 'users/user', user: @user %>
			<%= render 'books/form', book: @book %>
		</div>
		<div class="col-xs-9">
			<%= render 'books/list', books: @books %>
		</div>
	</div>
</div>